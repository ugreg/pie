{
  "name": "@ugreg/pi-agent-mon-repo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "permissions",
    "pkmn"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "npx oxlint permissions/ pkmn/",
    "build": "bun run --cwd permissions build && bun run --cwd pkmn build"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "oxlint": "^1.64.0"
  }
}
