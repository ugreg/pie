# Testing

## pi install locally:

no, pi install npm:pi-permission-system expects the package to be on the npm registry. for local testing:

```bash
# install from local tarball
pi install file:/Users/_/pie/permissions/ugreg-pi-permission-system-lite-0.1.0.tgz

# or from local directory
pi install file:/Users/_/pie/permissions
```
